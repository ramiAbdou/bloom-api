/**
 * @fileoverview Service: GoogleCalendar
 * - Note that this has nothing to do with any OAuth2 flow, but is rather used
 * for the Bloom service account, which schedules all events via Google
 * Calendar. In the future, there could be potential for something OAuth
 * related, but not currently
 * @author Rami Abdou
 */

export const addGuestToEvent = async () => {};

export const createEvent = async () => {};

export const deleteEvent = async () => {};

export const updateEvent = async () => {};
